<%= form_with :model => @password_request_form, :scope => :password_request, :url => password_request_path, :method => :post do |form| %>
  <p>If you have an account, enter your email here and we will send you a password reset link shortly!</p>
  <div class="form-group">
    <%= form.label :email, class: "control-label" %>
    <%= form.email_field :email, class: "form-control input-lg" %>
  </div>
  <div class="form-group">
    <%= form.submit "Request new password", class: "btn btn-lg btn-success" %>
  </div>
<% end %>
