- content_for :content do
  .toolbar
    nav
      ul.nav.nav-tabs
        - vehicle_links.each do |link|
          li class="#{active?(link.url)}"
            = link_to link.name, link.url
  = yield

= render file: 'layouts/application'
